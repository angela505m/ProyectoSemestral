name: petcare
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^1.1.0
  geolocator: ^10.1.0
  shared_preferences: ^2.2.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  flutter_local_notifications: ^17.1.2
  # timezone ya no es necesaria si no usamos zonedSchedule, pero la dejamos por si acaso
  timezone: ^0.9.2 
 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
 

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png